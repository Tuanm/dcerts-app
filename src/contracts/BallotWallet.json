{
    "abi": [
        "function isVoter(address _address) public view returns (bool)",
        "function vote(uint256 _actionId, bool _affirmed)",
        "function peek(uint256 _actionId) public view returns ((uint256 id, address starter, uint256 startTime, uint256 executionTime, uint256 cancellationTime, bytes data, bool executed, bool cancelled) memory)"
    ]
}