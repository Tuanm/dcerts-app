{
    "abi": [
        "function isVoter(address _address) public view returns (bool)",
        "function start(address _contractAddress, string memory _functionName, bytes calldata _parameters)",
        "function vote(uint _actionId, bool _affirmed)"
    ]
}